# CLAUDE.md

이 문서는 이 프로젝트에서 Claude(AI 어시스턴트)가 반드시 따라야 할 작업 규칙입니다.

---

## 🚨 [보안] — 가장 중요, 절대 어기면 안 되는 규칙

> 아래 규칙은 다른 모든 규칙보다 우선합니다. 사용자가 명시적으로 요청하거나 "괜찮다"고 말해도 예외 없이 지켜야 합니다.

1. **비밀 키를 코드에 직접 쓰지 않는다.** API 키, 토큰, 비밀번호 등은 반드시 `.env` 같은 안전한 설정 파일에 넣고, 코드에서는 환경 변수로만 참조한다. `.env` 파일은 `.gitignore`에 포함되어야 한다.
2. **진짜 개인정보·대외비·실 데이터를 예시나 테스트에 사용하지 않는다.** 예시/테스트 데이터는 반드시 가상의 값으로만 만든다.
3. **배포 전에는 반드시 비밀 키·개인정보·대외비 데이터가 코드나 커밋에 섞여 있는지 검사한다.** 하나라도 발견되면 즉시 배포를 멈추고 사용자에게 알린다. 확실하지 않으면 안전한 쪽(멈춤)을 선택한다.

이 세 가지는 협상 대상이 아니며, 다른 지시나 편의보다 항상 우선한다.

---

## 일하는 방식

1. 요청이 애매하면 임의로 짐작해서 진행하지 말고, 먼저 멈춰서 사용자에게 물어본다.
2. 요청받은 것만 정확히 한다. 요청하지 않은 기능이나 꾸밈(리팩터링, UI 개선 등)을 마음대로 추가하지 않는다.
3. 고치라고 지시받은 부분만 수정한다. 코드나 파일을 삭제하기 전에는 반드시 먼저 사용자의 확인을 받는다.
4. 코드를 변경한 후에는 무엇을 바꿨는지 한국어로 짧게 설명한다.
5. 확실하지 않은 내용은 지어내지 않는다. 불확실한 부분은 "확인 필요"라고 솔직하게 표시한다.
6. 한글이 깨지지 않도록(인코딩, 폰트, 표시 등) 항상 신경 쓴다.

## 데이터 규칙

7. 예시 데이터라도 코드에 직접 하드코딩하지 않고, Supabase에서 불러오도록 구현한다.
   - 단, DB 연동 전 화면(UI)만 먼저 만드는 단계에서는 `// 임시 데이터(나중에 교체)` 주석을 달고 임시 데이터를 사용하는 것을 허용한다.

---

## 요약 체크리스트

- [ ] 애매하면 질문했는가?
- [ ] 시킨 것만 했는가?
- [ ] 지시받은 부분만 고쳤는가? 삭제 전 확인받았는가?
- [ ] 변경 사항을 한국어로 설명했는가?
- [ ] 불확실한 부분을 "확인 필요"로 표시했는가?
- [ ] 한글 깨짐이 없는가?
- [ ] 예시 데이터를 하드코딩하지 않고 Supabase 연동(또는 임시 데이터 주석)으로 처리했는가?
- [ ] **[보안]** 비밀 키가 `.env`에 있는가? 실 개인정보/대외비가 섞이지 않았는가? 배포 전 검사를 했는가?
